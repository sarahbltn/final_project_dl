[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "final_project_ds"
version = "0.0.1"
description = "Sentiment Analysis in a personalized way."
authors = [
  { name = "Sarah Beltran, Monica Ibarra, David Gutierrez" },
]

readme = "README.md"
classifiers = [
    "Programming Language :: Python :: 3",
    
]
requires-python = "~=3.10.0"


[tool.ruff]
line-length = 99
src = ["final_project_ds"]
include = ["pyproject.toml", "final_project_ds/**/*.py"]

[tool.ruff.lint]
extend-select = ["I"]  # Add import sorting

[tool.ruff.lint.isort]
known-first-party = ["final_project_ds"]
force-sort-within-sections = true

